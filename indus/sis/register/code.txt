Create Login name and password:

1. SISselectlogin.jsp:-- use procedure, not select query.
	
2. SISupdateuserdetails.jsp: 
	a. out.println(con.getAutoCommit()); line no 156.
	b. use different try-catch for mysql if its fail show different message that sis 
	   created but hive user not created.
	c. activate only prod_category='MBA',e.enroll_status in ('ACTIVE', 'PAY_ERR'),
	   prod_group not in ('FORM SALE')

3. Remove all System.out.println.